const {getAllDetails} = require("./getAllDetails") 
const {deleteUserById} = require("./deleteDetails") 
const {getDetailbyId} = require("./getById") 

module.exports = {
    getAllDetails,
    deleteUserById,
    getDetailbyId
}